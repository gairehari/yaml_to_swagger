# yaml to swagger definitions converter
Documentation of yaml to swagger definitions converter

Usage:

python yaml_to_swagger.py YAML_FILE DEFINITION_OBJECT_NAME

e.g. python yaml_to_swagger.py ticket.yaml ticket
